services:
  - type: web
    name: OpenGraph Image Generator
    runtime: rust
    autoDeploy: true
    domain: opengraph.chriskrycho.com
    buildCommand: cargo build --release --locked
    startCommand: cargo run --release --locked
