services:
  - type: web
    name: OpenGraph Image Generator
    runtime: rust
    autoDeploy: true
    domain: opengraph.chriskrycho.com
    buildCommand: cargo build --release
    startCommand: ./target/run/opengraph-image
